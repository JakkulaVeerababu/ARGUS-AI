# Lightweight test-only requirements (no heavy ML deps)
# Heavy packages (faiss-cpu, sentence-transformers, etc.) are excluded
# as tests mock or stub these dependencies

# Core API framework
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.2
pydantic-settings==2.1.0

# Data processing
pandas==2.1.3
numpy==1.26.2

# Utilities
pyyaml==6.0.1
python-multipart==0.0.6
loguru==0.7.2
httpx==0.25.2

# Optional cache (tests work without Redis)
redis==5.0.1

# Testing
pytest==7.4.3
pytest-anyio==0.0.0
anyio==3.7.1
